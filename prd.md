##需求目标：

做一个记账管理系统。

##系统设计技术方案：
1、前端采用 react ，next.js 技术栈
2、后端采用 java  springboot 为技术栈

####接口设计：
    -add ： 增加一个记账信息
    -del ：删除一个记账信息
    -sum：计算当前账单总和

####数据结构设计
    账单：
        日期：日期字段
        金额：big字段
        内容：字符串

####数据存储在本地的csv文件，命名为 data.csv

##页面交互流程：
    -打开页面 展示 账单列表，每10页进行分页
    -页面底部展示 ： 增加，和计算总和按钮
        -点击增加： 新增三个输入框，进行填写，填写后保存，入库 data.csv
        -点击计算总和：计算当前总和，弹框提示金额内容
    -删除按钮放在每一条记录后，点击后，直接删除记录，删除 data.csv 中的内容

##设计原则：
    采取前后端分离设计，前端访问HTTP请求接口进行设计
    springboot分层 api service dao分层设计原则，避免暴露

##工程要求：
    整体工程放在 BASICJP 目录下
    考虑为后续CICD做准备要提交到 github来用
    其他需要安装部署的库和软件尽管安装

